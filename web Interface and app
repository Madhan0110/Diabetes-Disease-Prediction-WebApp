import numpy as np
import pickle
import streamlit as st

loaded_model = pickle.load(open( 'C:/Users/Hp/Desktop/trainedmodel/trained_model.sav', 'rb'))

#creating prediction
def diabetes_prediction(input_data):
    

    # changing the input_data to numpy array
    input_data_as_numpy_array = np.asarray(input_data)

    # reshape the array as we are predicting for one instance
    input_data_reshaped = input_data_as_numpy_array.reshape(1,-1)

    prediction = loaded_model.predict(input_data_reshaped)
    print(prediction)

    if (prediction[0] == 0):
      return 'The person is not diabetic'
    else:
      return 'The person is diabetic'
  
def main():
    
    # giving a title
    st.title('Diabetes Prediction Web App')
    
    # getting the input data from the user
    
    Pregnancies = st.text_input('Number of Pregnancies')
    Glucose = st.text_input('Glucose Level')
    BloodPressure = st.text_input('Blood Pressure value')
    SkinThickness = st.text_input('Skin Thickness value')
    Insulin = st.text_input('Insulin Level')
    BMI = st.text_input('BMI value')
    DiabetesPedigreeFunction = st.text_input('Diabetes Pedigree Function value')
    Age = st.text_input('Age of the Person')
    
    # code for Prediction
    diagnosis = ''
    
    # creating a button for Prediction
    
    if st.button('Diabetes Test Result'):
        diagnosis = diabetes_prediction([Pregnancies, Glucose, BloodPressure, SkinThickness, Insulin, BMI, DiabetesPedigreeFunction, Age])
        
    st.success(diagnosis)
    
if __name__ == '__main__':
    main()
    
    

"""                           
                                             ***DOCTORS PRESCRIPTION***    
                                             ***These are some common prescriptions for all types of diabetes:***
 
***Type 1 Diabetes:***

1.Insulin therapy: Typically, multiple daily injections of insulin or insulin pump therapy.

2.Blood glucose monitoring: Regular monitoring of blood sugar levels to adjust insulin doses.

3.Dietary management: Following a healthy diet plan to maintain blood sugar levels.

4.Exercise: Regular physical activity to help manage blood sugar levels and overall health.

5.Education and support: Diabetes education and support to learn about managing the condition.

***Type 2 Diabetes:***

1.Oral medications: Metformin is often the first-line medication, but other medications may be prescribed depending on the individual's needs.

2.Insulin therapy: Some individuals with type 2 diabetes may eventually need insulin therapy if other medications are not effective.

3.Blood glucose monitoring: Regular monitoring of blood sugar levels.

4.Dietary management: Following a healthy diet plan to maintain blood sugar levels.

5.Exercise: Regular physical activity to help manage blood sugar levels and overall health.

6.Education and support: Diabetes education and support to learn about managing the condition.

***Type3: Gestational Diabetes:***

1.Blood glucose monitoring: Regular monitoring of blood sugar levels.

2.Dietary management: Following a healthy diet plan to maintain blood sugar levels.

3.Exercise: Regular physical activity to help manage blood sugar levels and overall health.

4.Insulin therapy: Some women with gestational diabetes may require insulin therapy to manage blood sugar levels.

5.Education and support: Diabetes education and support to learn about managing the condition, especially during pregnancy.


***It's important for individuals with diabetes to work closely with their healthcare team to develop a personalized treatment plan.*** """
